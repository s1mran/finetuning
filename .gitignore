# training artifacts -- checkpoints and merged models are far too big for git.
# report.json stays tracked; the adapter and merged-model directories beside
# it under <TYPE>/reports/<run>/ do not.
runs/
outputs/
*/reports/*/*/
*.safetensors
*.bin
*.gguf

# python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# secrets -- never commit an HF token
.env
.env.*

# os
.DS_Store
